{
  "name": "@subso/link-parser",
  "version": "0.0.1",
  "description": "A directory of supported platforms for Notify with link parsing and metadata",
  "directories": {
    "test": "tests",
    "lib": "lib"
  },
  "scripts": {
    "test": "mocha -r ts-node/register tests/links.spec.ts",
    "build": "tsc",
    "build:watch": "tsc -w",
    "prepare": "tsc"
  },
  "author": "Notify Technology, Inc. <code@notify.me>",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.2.11",
    "@types/mocha": "^7.0.2",
    "chai": "^4.2.0",
    "mocha": "^7.2.0",
    "prettier": "^2.0.5",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.3"
  }
}
